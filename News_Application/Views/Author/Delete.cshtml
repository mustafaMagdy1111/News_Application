@model News_Application.Models.author


<div class="modal-content">
    <div class="modal-header">
        <h3>Are you sure you want to delete this?</h3>
        <button type="button" class="close" data-dismiss="modal" aria-label="Close">
            <span aria-hidden="true">×</span>
        </button>
    </div>


        <div class="modal-body">
           
            
            <div>
              
                <h4>Delete Author</h4>
                <hr />
                <dl class="dl-horizontal">
                    <dt>
                        @Html.DisplayNameFor(model => model.DateOfBirth)
                    </dt>

                    <dd>
                        @Html.DisplayFor(model => model.DateOfBirth)
                    </dd>

                    <dt>
                        @Html.DisplayNameFor(model => model.PhoneNumber)
                    </dt>

                    <dd>
                        @Html.DisplayFor(model => model.PhoneNumber)
                    </dd>

                    <dt>
                        @Html.DisplayNameFor(model => model.Email)
                    </dt>

                    <dd>
                        @Html.DisplayFor(model => model.Email)
                    </dd>

                </dl>
            </div>

        </div>
        <div class="modal-footer">
            @using (Html.BeginForm("Delete", "Author", FormMethod.Post, new { @class = "m-form m-form--fit m-form--label-align-right" }))
            {
                @Html.AntiForgeryToken()
                @Html.HiddenFor(model => model.Id)
                <button class="btn btn-primary" data-dismiss="modal">Close</button>
                <input id="DeleteGroup" class="btn btn-danger" type="submit" value="Submit" />
            }
        </div>
</div>
